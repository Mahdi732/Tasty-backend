// Optional job scaffold for future optimization:
// Move historical revoked sessions to archive storage if needed.
export const cleanupRevokedSessionsJob = async () => {
  return Promise.resolve();
};
